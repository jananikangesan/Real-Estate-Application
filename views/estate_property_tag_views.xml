<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Tag Tree -->
    <record id="view_estate_property_tag_tree" model="ir.ui.view">
        <field name="name">estate.property.tag.tree</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <tree string="Property Tags">
                <field name="name"/>
                <field name="color"/>
            </tree>
        </field>
    </record>

    <!-- Tag Form -->
    <record id="view_estate_property_tag_form" model="ir.ui.view">
        <field name="name">estate.property.tag.form</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <form string="Property Tag">
                <sheet>
                    <group col="2">
                        <field name="name" placeholder="Name" nolabel="1" colspan="2"/>
                        <field name="color"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_estate_property_tag_kanban" model="ir.ui.view">
        <field name="name">estate.property.tag.kanban</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                <field name="color"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card o_kanban_record">
                            <span t-attf-class="o_tag o_tag_color_#{record.color.raw_value}">
                                <t t-esc="record.name.value"/>
                            </span>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>




    <!-- Action -->
    <record id="action_estate_property_tag" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">kanban,tree,form</field>
    </record>

    <!-- Menu (under Settings) -->
    <menuitem id="menu_estate_property_tag"
              name="Property Tags"
              parent="menu_real_estate_settings"
              action="action_estate_property_tag"
              sequence="20"/>
</odoo>
